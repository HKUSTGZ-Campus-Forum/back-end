# Blocks the entire Better Auth namespace, including undocumented or future
# registration, password login, password reset, and session endpoints.
location ^~ /api/auth/ {
    access_log off;
    default_type application/json;
    return 404 '{"code":"legacy_auth_disabled"}';
}
